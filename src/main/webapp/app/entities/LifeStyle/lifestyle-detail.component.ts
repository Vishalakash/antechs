import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { ILifeStyle } from 'app/shared/model/lifestyle.model';

@Component({
  selector: 'jhi-lifestyle-detail',
  templateUrl: './lifestyle-detail.component.html',
})
export class LifeStyleDetailComponent implements OnInit {
    lifestyle: ILifeStyle | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ lifestyle }) => (this.lifestyle = lifestyle));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
