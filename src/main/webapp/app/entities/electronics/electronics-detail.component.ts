import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { IElectronics } from 'app/shared/model/electronics.model';

@Component({
  selector: 'jhi-electronics-detail',
  templateUrl: './electronics-detail.component.html',
})
export class ElectronicsDetailComponent implements OnInit {
  electronics: IElectronics | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ electronics }) => (this.electronics = electronics));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
